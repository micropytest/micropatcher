[project]
name = "micropatcher"
version = "0.1.0"
description = "A lib for patching things on MicroPython."
authors = []
requires-python = ">=3.14"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
  "mpremote (>=1.28.0,<2.0.0)",
  "pyright (>=1.1.410,<2.0.0)",
  "ruff (>=0.15.10,<1.0.0)",
]

[tool.poetry]
package-mode = false
